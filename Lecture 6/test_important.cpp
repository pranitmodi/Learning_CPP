#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a = pow(10,0);
    int b = pow(10,1);
    double c = pow(10,2);
    int d = (int) c;

    cout << a << endl << b << endl << c << endl;

    int e = 1;
    cout << (!e);

    return 0;
}