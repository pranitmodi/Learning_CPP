#include <bits/stdc++.h>
using namespace std;
int main(int n, int k)
{
    int n,k;
    vector<int> v(n-1,1);
    cin >> n;
    cin >> k;
    while(n == 1)
    {
        for(int i=1; i<=k; i++)
        {
            if(k > n)
            {
                int s = k%n;
                
            }
        }
    }
}